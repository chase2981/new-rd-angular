/* tslint:disable:no-unused-variable */

import { async, inject } from '@angular/core/testing';
import { ComboSelect } from './combo-select';

describe('ComboSelect', () => {
  it('should create an instance', () => {
    expect(new ComboSelect()).toBeTruthy();
  });
});
