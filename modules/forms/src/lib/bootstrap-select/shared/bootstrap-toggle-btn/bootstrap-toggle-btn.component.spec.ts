/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { BootstrapToggleBtnComponent } from './bootstrap-toggle-btn.component';

describe('Component: BootstrapToggleBtn', () => {
  it('should create an instance', () => {
    let component = new BootstrapToggleBtnComponent();
    expect(component).toBeTruthy();
  });
});
